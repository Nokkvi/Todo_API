# import User from "./generated/prisma.graphql"
# import Todo from "./generated/prisma.graphql"

type Query {
  info: String!
  users: [User!]!
  user(id: ID!) : User
}

type Mutation {
  createUser(name: String!, email: String!, password: String!) : User
  createTodo(text: String!, userId: ID!, dueAt: DateTime): Todo
  
  changePassword(userId: ID!, password: String!) : User
  #editTodo(text: String!, done: Boolean, userId: ID): User

  deleteUser(userId: ID!) : User
  #deleteTodo(text: String!, userId: ID) : Boolean
}